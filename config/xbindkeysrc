# Middle click (scroll wheel press) — handled by middle-click-hold.py
# (holds Space while button is held, for Claude Code voice input)

# Back side button — Enter key
"xdotool key Return"
  b:8

# Forward side button — toggle transparency
"$HOME/Documents/47industries/toggle-transparency.sh"
  b:9
